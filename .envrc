# Activate virtual environment via direnv (env-var approach, no source activate)
export VIRTUAL_ENV=/home/sean/SCRIBE/.venv
PATH_add "$VIRTUAL_ENV/bin"

# Add scripts directory to PATH
PATH_add scripts
